# API mode is used by scripts/start-mvp.ps1. Use mock only for isolated frontend work.
VITE_DATA_SOURCE=api

# Used only when VITE_DATA_SOURCE=api. This is browser-visible, so never put secrets here.
VITE_BACKEND_URL=http://localhost:8000
