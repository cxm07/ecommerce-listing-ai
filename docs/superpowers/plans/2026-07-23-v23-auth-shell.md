# V23 登录与应用壳 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deliver the F1 frontend authentication mock, protected application shell, role display and HTTP authorization UX without changing backend or public contracts.

**Architecture:** Add a frontend-only `AuthRepository` boundary backed by the V23 authenticated-user fixture, then expose the session through React context. Route protection, login restoration and authorization errors consume that context; existing V1 task repository remains the sole workflow data boundary.

**Tech Stack:** React 19, TypeScript, React Router 7, Vitest, Testing Library, existing CSS.

## Global Constraints

- Modify only frontend files and F1 design/plan documentation.
- Do not modify `backend/**`, `supabase/**`, V1 workflow states or public API documents.
- Do not add Supabase SDK, credentials, service-role keys or fake server login endpoints.
- Keep Mock and API behavior behind repository interfaces.

---

### Task 1: Define the authentication boundary and fixture-backed mock

**Files:**

- Create: `frontend/src/auth/contracts.ts`
- Create: `frontend/src/auth/mockAuthRepository.ts`
- Create: `frontend/src/auth/authRepository.test.ts`

**Produces:** `AuthUser`, `AuthSession`, `AuthRepository`, and a mock that reads the V23 identity fixture and supports explicit sign-in/sign-out.

- [ ] Write tests proving a signed-out mock returns no session, successful sign-in returns the V23 operator identity, and sign-out clears it.
- [ ] Run `npm test -- --run src/auth/authRepository.test.ts`; confirm failure because the module is absent.
- [ ] Implement the smallest typed repository and fixture-backed mock needed by those tests.
- [ ] Re-run the focused test and confirm all tests pass.

### Task 2: Add provider, session lifecycle, and authorization event bridge

**Files:**

- Create: `frontend/src/auth/AuthProvider.tsx`
- Create: `frontend/src/auth/authEvents.ts`
- Create: `frontend/src/auth/AuthProvider.test.tsx`
- Modify: `frontend/src/data/httpTaskRepository.ts`
- Modify: `frontend/src/data/httpTaskRepository.test.ts`

**Consumes:** `AuthRepository` and `ApiResponse` envelopes.

**Produces:** `useAuth`, loading/ready/authenticated session states, explicit sign-out, and a browser-safe unauthorized event dispatched only for HTTP 401 or `AUTHENTICATION_REQUIRED`.

- [ ] Write tests for provider loading, successful sign-in, sign-out, and unauthorized-event session clearing.
- [ ] Add a failing HTTP repository test showing an authorization envelope emits the unauthorized signal instead of being treated as a network failure.
- [ ] Run focused tests; confirm expected missing-module or assertion failures.
- [ ] Implement provider/context and minimal HTTP event notification.
- [ ] Re-run focused tests and confirm green.

### Task 3: Protect routes and build F1 authentication pages

**Files:**

- Create: `frontend/src/auth/ProtectedRoute.tsx`
- Create: `frontend/src/auth/PermissionGuard.tsx`
- Create: `frontend/src/auth/AuthRoutes.test.tsx`
- Modify: `frontend/src/App.tsx`
- Modify: `frontend/src/pages.tsx`
- Modify: `frontend/src/styles.css`

**Consumes:** `useAuth`, React Router location state/search parameters, and V23 roles `operator`, `reviewer`, `admin`.

**Produces:** protected task routes; login return-to restoration; role/user/exit controls in the shell; loading, forbidden and accessible error presentation.

- [ ] Write route tests for unauthenticated redirect, safe return-to restoration, logout protection, 403 page, role badge, and disabled permission explanation.
- [ ] Run the focused route test and confirm failure before route/auth UI implementation.
- [ ] Wrap routes in `AuthProvider`, add `ProtectedRoute`, replace the V1 login placeholder, add forbidden UI, and render user/role controls in the shell.
- [ ] Add CSS only for the new auth shell, form states, badges, focus indicators, and responsive behavior.
- [ ] Re-run focused tests and confirm green.

### Task 4: Verify the F1 slice and prepare the reviewable handoff

**Files:**

- Modify: `docs/superpowers/specs/2026-07-23-v23-auth-shell-design.md`
- Modify: `docs/superpowers/plans/2026-07-23-v23-auth-shell.md`

- [ ] Run `npm test -- --run` from `frontend`; expect all V1 and F1 tests to pass.
- [ ] Run `npm run build` from `frontend`; expect a successful production build.
- [ ] Run `pytest -q` from `backend`; expect the V1 backend regression suite to pass unchanged.
- [ ] Run `git diff --check` and inspect `git status --short`; confirm only F1 frontend/docs files are staged for the PR.
- [ ] Commit with `feat: add V23 auth shell`.
- [ ] Push `feature/v23-auth-shell` and create a Draft PR that states mock-only auth, no backend changes, test evidence, V23 fixture dependency, and the F2 contract mismatch discovered in F0.
